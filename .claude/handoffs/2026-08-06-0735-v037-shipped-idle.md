# Handoff: 0.37.0 shipped (codex responses-lite + exit auto-merge), idle

> 2026-08-06 · main @ 39de9cb · 1 dirty file (this handoff)

## Goal

Eric's two 2026-08-06 reports, both fixed and released as 0.37.0:
1. "totally wrong session re-construction for codex" (the deepseek-cli
   trace, reference/deepseek-cli/.cctrace/trace-2026-08-06T08-00-14.jsonl)
   — codex >= ~0.146 responses-lite protocol broke the dialect adapter.
2. "huge session closing wait too long and no progress ... like stucking"
   — the exit auto-merge parsed the whole log dir silently.

## State

Released, published, verified. No work in flight.

- v0.37.0 = PR #88, squash-merged as 39de9cb; tag + GitHub release up;
  npm live (npm view -> 0.37.0, shasum 94fc7ed39f8f...); local binary
  `cctrace --version` -> v0.37.0 (39de9cb); update-check cache reset.
- Codex responses-lite (src/dialects/openai.ts): completed events carry
  usage but output:[] — items grafted back from output_item.done;
  output_text.delta assembly for truncated streams; the leading
  developer-message run folds into the system prompt; AGENTS.md /
  environment_context wrappers skipped by turnSnippet +
  openaiFirstUserText. Verified against the real trace: 50/50 assistant
  turns attributed, real prompt as thread/registry identity, final
  answer renders.
- Exit auto-merge (planMerge/applyMerge in src/storage.ts,
  mergeProgressPrinter in src/cli.ts): scoped plans substring-scan sids
  before parsing — 2.5GB dir went ~22s/7GB RSS silent -> 5.9s/2.5GB
  with progress; files >=16MB print scan/read/write lines headed by a
  ctrl-c-is-safe note; 8MB chunked writes; cctrace merge same printer.
- Tests: bun test -> 623 pass, 0 fail. Devlogs (gitignored):
  docs/devlog/2026-08-06-codex-responses-lite-wire-facts.org,
  docs/devlog/2026-08-06-exit-auto-merge-stuck.org.
- Memory: cctrace-release-flow.md carries the 0.37.0 record.
- Only dirty file is .claude/handoffs/latest.md (rides the next PR,
  the established pattern).

## Next

1. Nothing queued. When Eric returns, standing candidates: settings
   panel phase 1 (#85, docs/design/settings.md — /api/config + gear
   icon) is the designed-and-waiting feature.
2. If a codex compaction is ever captured under responses-lite, check
   session.ts contOf's marker gate against its shape.
3. cctrace compress at zstd-19 on GBs is minutes and silent — apply the
   MergeProgress printer pattern if it ever bites.

## Don't repeat

- Wrapper-skip list (turnSnippet/openaiFirstUserText) stays explicit —
  a generic "xml-tag-wrapped = harness" rule would eat user-pasted XML.
- reference/ traces quote real work: never copy their strings into
  tracked files (a real prompt landed in a test fixture this session;
  caught pre-commit, replaced with synthetic text).
- Scoped-merge substring scan must never under-count: a wire sid is
  always a verbatim substring of its pair's JSON line — no partial
  reads.
- Still vetoed from earlier rounds: portless-by-default, capture-time
  sid masking, dashboard write access to capture config.

## Read first

1. CHANGELOG.md 0.37.0 — what shipped
2. docs/devlog/2026-08-06-*.org — the two decision chains
3. memory/cctrace-release-flow.md — release mechanics + records

## Verify

git rev-parse --short HEAD              # expect 39de9cb
git status --porcelain | wc -l          # expect 1 (handoffs/latest.md)
bun test 2>&1 | tail -3                 # expect 623 pass, 0 fail
cctrace --version                       # expect v0.37.0 (39de9cb)
npm view @thevibeworks/cctrace version  # expect 0.37.0
