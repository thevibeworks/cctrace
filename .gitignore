# captured traffic (contains real session + quota data — never commit)
.claude/CLAUDE.md
.deva
.cctrace/
.cctrace-*/
.claude-trace/
# manual-test workspace: example traces, analyze logs, scratch projects
temp/
temp-*/
# internal notes (raw backlog, not part of the published repo)
docs/internal/
# local agent tool checkouts
agenttools/
# MITM CA + leaf keys (private keys — never commit)
.cache/mitm/
# test run output (real captures + logs)
test-output/

# dependencies (bun install; bun.lock is the lockfile — no npm lockfile)
node_modules
package-lock.json

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# local reference checkouts (not part of this repo)
reference/
